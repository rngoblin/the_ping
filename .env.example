NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# V0.3 still uses mock realtime by default.
# Future Supabase realtime should replace services/realtime/mockRealtime.ts behind the same adapter interface.
